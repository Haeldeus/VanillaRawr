package math;

import java.util.ArrayList;

import util.Item;
//import util.MultiBonus; //Later used to create Item variation

/**
 * The Class, that will perform the main Calculation to get the best possible Equipment.
 * @author Haeldeus
 * @version 1.0
 */
public class MainMath {

  /**
   * Calculates the amount of Mana, the Character has .
   * @return The amount of Mana, this Character has as an Integer.
   * @since 1.0
   */
  private static int getMana(/*TODO*/) {
    
    return 0;
  }
  
  /**
   * The main Calculation for Items.
   * @param items All Items, that are available.
   * @param talents The Talents, this Character has selected.
   * @param c The Class, this Character is.
   * @since 1.0
   */
  public static void doMath(ArrayList<Item> items, int[] talents, util.Class c) {
    getMana();
  }
}
